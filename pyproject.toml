[project]
name = "reinforcement-learning-maze-solver"
version = "0.1.0"
description = "Gridworld / Maze Solver with Q-Learning and SARSA"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
